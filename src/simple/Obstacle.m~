%this is a wrapper class for specific obstacle classes. 
%maybe some generic methods will be available here later...
classdef Obstacle
    methods
        function o = Obstacle()
        end
    end
end
